name: Prettier

on:
  pull_request:
    branches:
      - main
      - master
  push:
    branches:
      - main
      - master
    paths:
      - "index.html"
      - "assets/site/**"
      - "README.md"
      - ".github/workflows/**"

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4

      - name: Run Prettier
        run: npx prettier@3.3.3 --check index.html assets/site/main.js assets/site/styles.css README.md .github/workflows/*.yml
